const casperClientSDK = require('casper-js-sdk') // import casper-js-sdk
const {
  Keys,
  CasperClient,
  CLPublicKey,
  DeployUtil,
  CLValueBuilder,
  RuntimeArgs,
} = require('casper-js-sdk')

module.exports = {
  casperClientSDK,
  Keys,
  CasperClient,
  CLPublicKey,
  DeployUtil,
  CLValueBuilder,
  RuntimeArgs,
}
